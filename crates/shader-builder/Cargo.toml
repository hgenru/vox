[package]
name = "shader-builder"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"

[build-dependencies]
spirv-builder = { git = "https://github.com/Rust-GPU/rust-gpu.git", branch = "main" }
