[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { workspace = true, features = ["std"] }
serde = { workspace = true }
bitcode = { workspace = true }
glam = { workspace = true }
bytemuck = { workspace = true }
